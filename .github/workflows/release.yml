name: release

# Tags stay for human-cut releases. The manifest path trigger is what makes the
# pipeline reach users: upstream can rotate bytes without changing Version, so a
# tag-only scheme would collide on an existing tag and skip a payload users
# need. Publishing on content change handles that; the kill switch gates it.
on:
  push:
    tags: ['v*']
    branches: [main]
    paths:
      - 'io.github.vivienm.ChatGPT.yaml'
  workflow_dispatch:

permissions:
  contents: write
  packages: write
  pages: write
  id-token: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  build:
    name: build (${{ matrix.arch }})
    if: >-
      startsWith(github.ref, 'refs/tags/') ||
      github.event_name == 'workflow_dispatch' ||
      vars.AUTO_UPDATE == 'on'
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-24.04
          - arch: aarch64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    container:
      image: ghcr.io/flathub-infra/flatpak-github-actions:freedesktop-25.08@sha256:6f3180c6765cb55e5dcd8ee4127b82aba25163c8c655a161422b7c447c14e4af
      options: --privileged
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      # Unsigned here. Signing happens once, in publish, so the key touches one
      # job rather than every matrix leg.
      - name: build
        run: |
          set -euo pipefail
          flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
          flatpak-builder --user --install-deps-from=flathub --force-clean \
            --disable-rofiles-fuse --arch=${{ matrix.arch }} \
            --repo=repo build io.github.vivienm.ChatGPT.yaml

      # Same gate as build.yml. A release must not publish bytes that were never
      # installed, which is how the tag path used to work.
      - name: smoke test
        run: |
          set -euo pipefail
          flatpak remote-add --user --if-not-exists --no-gpg-verify chatgpt-rel "$PWD/repo"
          flatpak install --user -y chatgpt-rel io.github.vivienm.ChatGPT
          scripts/ci-smoke.sh io.github.vivienm.ChatGPT

      # Tarred, not uploaded as a directory tree. upload-artifact walks files
      # and so drops empty directories, but an OSTree repo carries five that
      # are load-bearing (extensions, refs/mirrors, refs/remotes, state,
      # tmp/cache). Without them `ostree refs` fails with
      # "opendir(refs/remotes)" on the far side and `ostree init` does not
      # repair it, so the merge would fail on the first ever run.
      - name: pack the repo
        run: tar -cf repo.tar repo

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: repo-${{ matrix.arch }}
          path: repo.tar
          retention-days: 1

  publish:
    needs: build
    runs-on: ubuntu-24.04
    outputs:
      version: ${{ steps.ver.outputs.v }}
    container:
      image: ghcr.io/flathub-infra/flatpak-github-actions:freedesktop-25.08@sha256:6f3180c6765cb55e5dcd8ee4127b82aba25163c8c655a161422b7c447c14e4af
      options: --privileged
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: arch-repos

      - name: read version
        id: ver
        run: |
          set -euo pipefail
          ver=$(sed -n 's/.*<release version="\([^"]*\)".*/\1/p' build-aux/io.github.vivienm.ChatGPT.metainfo.xml | head -n1)
          [[ "$ver" =~ ^[0-9][-0-9A-Za-z.+:~]{0,127}$ ]] || {
            echo "invalid metainfo version" >&2; exit 1; }
          printf 'v=%s\n' "$ver" >> "$GITHUB_OUTPUT"

      # Before anything is signed, uploaded or pushed. The release, the GHCR
      # tags and the Pages site all carry the metainfo version, so a tag that
      # disagrees with it would publish under a version nobody authorised.
      # Manual dispatch keeps the derived behaviour and skips this.
      - name: tag must match the manifest version
        if: startsWith(github.ref, 'refs/tags/')
        env:
          REF_NAME: ${{ github.ref_name }}
          RELEASE_VERSION: ${{ steps.ver.outputs.v }}
        run: |
          set -euo pipefail
          want="v$RELEASE_VERSION"
          test "$REF_NAME" = "$want" || {
            echo "tag does not match metainfo $want"; exit 1; }

      # GPG signing is not optional: flatpak refuses system-wide installs of
      # extra-data apps from a source that is not gpg-verified.
      - name: import signing key
        env:
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
        run: |
          set -euo pipefail
          export GNUPGHOME=$PWD/.gnupg
          mkdir -p -m700 "$GNUPGHOME"
          echo "$GPG_PRIVATE_KEY" | gpg --batch --import
          echo "GNUPGHOME=$GNUPGHOME" >> "$GITHUB_ENV"

      # Named `repo` because publish-pages.sh copies that path unchanged.
      - name: merge and sign
        env:
          GPG_KEY_ID: ${{ secrets.GPG_KEY_ID }}
        run: |
          set -euo pipefail
          for arch in x86_64 aarch64; do
            tar -xf "arch-repos/repo-$arch/repo.tar" -C "arch-repos/repo-$arch"
          done
          ./scripts/merge-repos.sh repo \
            arch-repos/repo-x86_64/repo arch-repos/repo-aarch64/repo
          # Creates the appstream refs, so it runs before the signing pass.
          # No deltas yet: a static delta embeds the commit it was generated
          # from, so one built before signing hands the client a commit with no
          # detached metadata, and the install fails "no signatures found" even
          # though the standalone commit object is signed.
          flatpak build-update-repo --gpg-sign="$GPG_KEY_ID" repo/
          # build-update-repo signs the summary, not the commits, and
          # merge-repos.sh copies commits verbatim through `ostree pull-local`,
          # which preserves their absent signatures. Without this the remote
          # advertises a GPGKey, every client turns commit verification on, and
          # the install dies with "no signatures found" after the summary has
          # already verified fine. Signing a signed commit errors, so skip the
          # ones build-update-repo made itself.
          ./scripts/sign-repo.sh repo "$GPG_KEY_ID"
          # Deltas last, so they carry signed commits, and the summary that
          # advertises them is regenerated in the same pass.
          flatpak build-update-repo --gpg-sign="$GPG_KEY_ID" \
            --generate-static-deltas --prune repo/
          ./scripts/sign-repo.sh repo "$GPG_KEY_ID"
          echo "published refs:"; ostree --repo=repo refs | grep '^app/'

      # No sideload bundle: build-bundle cannot carry an extra-data source
      # (flatpak#1334), so the .flatpak it produces has no application in it.

      - name: assemble Pages site
        env:
          GPG_KEY_ID: ${{ secrets.GPG_KEY_ID }}
          PAGES_URL: https://vivienm.github.io/chatgpt-flatpak
          BUILD_ID: ${{ github.run_id }}-${{ github.run_attempt }}
        run: ./scripts/publish-pages.sh

      # The public key has already been exported into the Pages metadata. Drop
      # the private key before any later third-party action executes.
      - name: remove signing key
        if: always()
        run: |
          if [ -n "${GNUPGHOME:-}" ]; then
            rm -rf -- "$GNUPGHOME"
          fi
      # upload-pages-artifact internally calls upload-artifact@v4 (unpinned tag),
      # which the repository's Actions policy rejects. Inline its steps instead:
      # create the tar archive manually and upload it via the already-allowed
      # upload-artifact action.
      - name: archive Pages artifact
        run: |
          tar \
            --dereference --hard-dereference \
            --directory public \
            -cf "$RUNNER_TEMP/artifact.tar" \
            --exclude=.git \
            --exclude=.github \
            .

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: github-pages
          path: ${{ runner.temp }}/artifact.tar
          retention-days: 1
          if-no-files-found: error

      # Built here because only this container has flatpak, but pushed from a
      # separate job. This image ships no registry client at all, no skopeo,
      # oras, crane, podman or docker, and no package manager to add one. The
      # inline `skopeo copy` that used to live here had never executed and died
      # with "skopeo: command not found" the first time it ran.
      - name: build the OCI image
        run: |
          set -euo pipefail
          flatpak build-bundle --oci --oci-layer-compress=zstd \
            repo oci-image io.github.vivienm.ChatGPT

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: oci-image
          path: oci-image
          retention-days: 1

      # Cut before the GHCR push, not after. The release is the cheaper thing to
      # get right and OCI is the optional channel; when the push failed on the
      # first real run it skipped the release as well, which is the wrong order
      # of losses.
      #
      # No files: attached, per the bundle note above. The release is a marker
      # pointing at the repo remote.
      - uses: softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 # v3.0.3
        with:
          tag_name: v${{ steps.ver.outputs.v }}
          body: |
            Packaging build for upstream ChatGPT ${{ steps.ver.outputs.v }}.

            Install:
                flatpak remote-add --user chatgpt https://vivienm.github.io/chatgpt-flatpak/chatgpt.flatpakrepo
                flatpak install --user chatgpt io.github.vivienm.ChatGPT

  # Plain ubuntu-24.04, not the flatpak container, because that container has no
  # registry client and no package manager to install one. Runs after publish, so
  # a failure here costs only the OCI channel: Pages and the release are done.
  ghcr:
    needs: publish
    runs-on: ubuntu-24.04
    permissions:
      packages: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: oci-image
          path: oci-image

      - name: push to GHCR
        env:
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GHCR_USER: ${{ github.actor }}
          RELEASE_VERSION: ${{ needs.publish.outputs.version }}
          REPOSITORY_OWNER: ${{ github.repository_owner }}
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y skopeo
          echo "$GHCR_TOKEN" | skopeo login ghcr.io \
            -u "$GHCR_USER" --password-stdin
          for tag in "$RELEASE_VERSION" latest; do
            skopeo copy --format oci oci:oci-image \
              "docker://ghcr.io/$REPOSITORY_OWNER/chatgpt-flatpak:$tag"
          done
          echo "a newly created GHCR package is private; make it public in the"
          echo "package settings or anonymous pulls get 401"

  deploy-pages:
    needs: publish
    runs-on: ubuntu-24.04
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0

  # The only job that tests what a user actually does. Every other install in
  # this pipeline reads a local repo directory with --no-gpg-verify, which is
  # how a remote with signed summary and unsigned commits went out green: the
  # smoke test disabled the exact check that was broken. This one installs over
  # HTTPS from the published URL with verification left on, so a signing
  # regression fails the release instead of reaching users.
  #
  # Last, and allowed to fail loudly, because by this point the bytes are
  # already public. It reports rather than prevents.
  verify-published-remote:
    needs: [publish, deploy-pages]
    runs-on: ubuntu-24.04
    steps:
      # ci-smoke.sh reads build-aux/*.metainfo.xml to check the installed
      # payload against the version this release claims, so it needs the tree.
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - name: install from the published remote
        env:
          REPO_URL: https://vivienm.github.io/chatgpt-flatpak/chatgpt.flatpakrepo
          STAMP_URL: https://vivienm.github.io/chatgpt-flatpak/build-id
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y flatpak

          # Wait for THIS run's deploy, not merely for a 200. Polling for
          # reachability passes instantly against the previous deploy, which is
          # how a green verify job once reported on bytes it never fetched.
          want="${{ github.run_id }}-${{ github.run_attempt }}"
          for _ in $(seq 1 40); do
            got=$(curl -fsS "$STAMP_URL" 2>/dev/null || true)
            [ "$got" = "$want" ] && break
            sleep 15
          done
          [ "${got:-}" = "$want" ] || {
            echo "Pages still serving '$got', expected '$want' after 10 minutes"
            exit 1
          }

          flatpak remote-add --user --if-not-exists flathub \
            https://flathub.org/repo/flathub.flatpakrepo
          flatpak remote-add --user chatgpt "$REPO_URL"
          # No --no-gpg-verify. That omission is the whole point of this job.
          flatpak install --user -y chatgpt io.github.vivienm.ChatGPT
          scripts/ci-smoke.sh io.github.vivienm.ChatGPT
          echo "verified: installs from the published remote with GPG on"
